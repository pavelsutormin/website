{
  "name": "website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --strictPort --port 8080",
    "build": "vite build --minify esbuild --emptyOutDir",
    "preview": "vite preview --strictPort --port 8080"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "firebase": "^11.5.0",
    "lorem-ipsum": "^2.0.8"
  }
}
